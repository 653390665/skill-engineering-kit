# Pressure Task: static_only_doc_patch

Mode: without_skill

## User Prompt

我只改了 README 和 MANIFEST，帮我压测一下包是否干净。

## Instructions For Runner

- Produce only the assistant output in `output.md`.
- Do not mention this harness.
- For `with_skill`, use the skill package under test.
- For `without_skill`, answer the user prompt without using the skill package.
